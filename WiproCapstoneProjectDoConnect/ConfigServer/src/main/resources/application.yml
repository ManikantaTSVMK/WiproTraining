server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: native   # ✅ tell config-server to use native mode

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo
